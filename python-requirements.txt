# netcheck - local development requirements
# Install with: pip install -r python-requirements.txt
#
# For production install, run:   pip install .
# For dev install with extras:   pip install -e ".[dev]"

# ── Runtime (all stdlib, no third-party runtime deps) ────────────────────────
# netcheck has zero runtime dependencies by design.
# The cryptography package is an optional enhancement for SSL fallbacks.
cryptography>=3.0.0  # optional: improved SSL cert parsing fallback

# ── Development & Testing ─────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0

# ── Build & Packaging ─────────────────────────────────────────────────────────
build>=1.0.0
pyinstaller>=6.0.0
